# Step 3 integrations and Phase 2 prep

This archive adds:
- OpenClaw adapter skeleton
- Consent API skeleton
- Behavior API skeleton
- Storage abstraction (MinIO, S3, WebDAV placeholders)

## How to use
1. Extract this ZIP into the root of your local `PLATFORM` repo.
2. Overwrite files if asked.
3. Commit and push:

   git add .
   git commit -m "Add Step 3 integration skeletons"
   git push origin main
