# Step 5 permissions, audit, storage and plugin workflow

This archive adds:
- role/permission guards
- audit log service
- storage abstraction factory/providers
- upgraded OpenClaw adapter
- plugin approval/install workflow guards
- config for storage/OpenClaw env vars

## How to use
1. Extract this ZIP into the root of your local `PLATFORM` repo.
2. Overwrite files if asked.
3. Commit and push:

   git add .
   git commit -m "Add Step 5 permissions, audit, storage, and workflow"
   git push origin main
